import { type Metadata } from "next";

export { default } from "@/components/Playground/PlaygroundLayout";

export const metadata: Metadata = {
  title: "Playground - react-marked-renderer",
  description:
    "A markdown preview playground for react-marked-renderer which allows rendering markdown as React components using marked.",
};
