import { type ReactElement, type ReactNode } from "react";

import { type RenderTableSectionProps } from "../types.js";

export function RenderTbody(
  props: Readonly<RenderTableSectionProps>
): ReactElement {
  return <></>;
}
